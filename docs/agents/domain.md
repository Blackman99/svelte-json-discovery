# Domain Documentation

The canonical domain vocabulary and product boundaries live in the root
`CONTEXT.md`.

Architectural decisions that need durable rationale belong in `docs/adr/`.
Use one ADR per decision and link it from the relevant spec issue. This project
currently uses a single root context because the docs site and published package
serve one product domain.
