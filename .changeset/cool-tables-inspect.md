---
'svelte-json-discovery': minor
---

Add an automatic object-array Table Inspector view with deterministic loaded-window
columns, compact nested cells, shared path selection and search metadata, plus
bounded incremental row loading.
